services:
  - type: web
    name: identificador-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app_api:app --host 0.0.0.0 --port 10000
    autoDeploy: true
